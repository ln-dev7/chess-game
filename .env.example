# Promo code awarded when a player defeats the LN GM AI in standard chess.
# Exposed only through /api/winner-code, after the server re-validates the
# winning conditions (no NEXT_PUBLIC_ prefix → never bundled in the client).
LN_CHESS_GAME_WINNER_CODE=
